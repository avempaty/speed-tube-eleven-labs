/** @jest-environment setup-polly-jest/jest-environment-node */

// import { YouTubeV1 } from "../YouTubeAPI"
// import setupPollyNode from "../../common/test/setupPollyNode"

describe.skip("YoutubeAPI", () => {
  // let context = setupPollyNode(__dirname)

  it("should scrape transcript", async () => {
    // context.polly.configure({
    //   recordIfMissing: true,
    // })
    // const script = await YouTubeV1.scrapeTranscript("DR_yTQ0SYVA")
    // expect(script).toMatchSnapshot()
  })
})
